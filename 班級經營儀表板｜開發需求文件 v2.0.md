# 班級經營儀表板｜開發需求文件 v2.0

## 0. 文件資訊

- 版本：2.0
- 日期：2026-09-02
- 狀態：MVP 開發基準草案
- 前一版本：v0.1

### 0.1 本次主要更新

v2.0 保留 v0.1 的教育理念與核心機制，並正式加入：

1. 跨裝置使用需求。
2. 教師後台與學生前台雙介面。
3. Google Sheet 雲端資料層。
4. Google Apps Script 後端服務。
5. 同一堂課在多裝置間的狀態同步。
6. 教師權限、學生前台唯讀權限與顯示代碼。
7. 離線、同步失敗、重複操作與寫入衝突處理。
8. 可直接作為開發與驗收依據的資料結構、流程和完成條件。
9. 每學期正式 Google Sheet 名單的匯入、驗證與差異更新流程。

### 0.2 前端原型開發階段確認事項（2026-09-02 補充）

實際著手開發教師控制台與學生前台的前端原型時，確認了本教室的部署方式，並對下列項目做出調整。細節見對應章節，此處只列重點：

1. **單一鏡射螢幕**：本教室是教師筆電直接鏡射投影，沒有獨立的投影裝置，因此 MVP 第一版把教師控制台與學生前台整合成同一個網頁，預設顯示學生前台內容，教師以簡易密碼解鎖進入控制台操作，操作完成後手動切回投影畫面（詳見五、6.2、7.4、7.5）。原本雙裝置、雙網址的設計仍保留在文件中，作為未來若改用獨立投影裝置時的可選架構，不是被取代。
2. **💡 深度提問操作簡化**：教師端改為單一按鈕，每點擊一次新增一筆，不再另外提供「－💡」按鈕；誤按時透過既有的「復原最近一次操作」機制修正（詳見十一）。
3. **教師登入現況**：前端原型階段，教師登入暫時用寫死在程式碼裡的示範密碼取代，只用來擋學生誤觸教師控制台，不具備真正的帳號驗證效力。待接上正式後端後，仍必須換成第 19.1 節所述的 Google 帳號授權機制，這點不受本次前端原型影響。
4. **後端平台尚未選定**：Google Apps Script、Firebase、Supabase 等方案都還在考慮，目前先以瀏覽器本機資料模擬課堂資料（不是正式資料層），優先驗證前端操作流程與版面配置是否好用。
5. **歷史課堂清單尚未實作**：目前前端原型的本機資料層（localStorage）只保留「當下這一堂課」，換下一堂課就覆蓋掉，還沒有累積多堂課的歷史清單。燈號統計（7.7.1）、歷史紀錄（7.6）、學期摘要（7.7）這三個頁面都需要先有歷史課堂清單才能實作，列為後續前端工作；本次僅先把統計的設計方式寫進文件，不影響已定案的 `Sessions` 資料結構（17.3）。
6. **燈號紀錄改為不限筆數、自然增長**：原規格假設一堂課固定最多三格燈號。實作後認為「固定三階段」是人為限制，一堂課秩序狀況可能切換不只三次，改為每次點擊都新增一筆帶時間戳記的紀錄，不限筆數；如需更正歷史某一筆，改為點選該筆紀錄後選擇正確顏色，不會另外增加一筆。對應章節：九、7.4、7.7.1、17.3、廿一。
7. **新增「特殊表現球」🎱**：在小組層級的星級、💡 之外，另外提供個別學生的表揚機制，用座號輸入、不需要學生名冊，隨堂課結束清空。詳見新增的「特殊表現球」章節（廿九）。

---

# 一、產品定位

建立一套適合國小資訊課使用的「班級經營儀表板」。

系統用來解決兩類課堂需求：

1. 讓學生即時知道目前班級秩序狀況。
2. 降低學生遇到一般問題時立即依賴教師的習慣，逐步建立閱讀、嘗試、小組互助與高品質提問。

系統的目的不是單純讓學生少問問題，而是：

> 提升學生自主處理問題與有效求助的能力。

系統不是扣分工具，也不將需要協助視為失敗。它是一套把班級秩序與自主求助方式外部化、視覺化的課堂管理工具。

---

# 二、核心教育原則

## 2.1 班級秩序與自主求助必須分開

### A. 班級秩序燈號

用途：呈現目前全班秩序狀態。

- 🟢 綠燈
- 🟡 黃燈
- 🔴 紅燈

一節課最多產生三次正式燈號紀錄。燈號與學生是否提問沒有直接關係。
但過程中可能有燈號的轉換，必須思考這些轉換也是記錄的一環嗎？

### B. 小組自主解題狀態

用途：呈現各小組目前處理問題、互助與尋求協助的狀況。

- ⭐⭐⭐ 自主合作
- ⭐⭐ 努力發展中
- ⭐ 需要較多支持

星星代表可變動的「目前狀態」，不是可以被消耗的點數。不得使用「問一次老師就扣一顆星」等規則。

## 2.2 狀態可以恢復

如果小組開始閱讀提示、自主嘗試、互相協助或改善提問方式，狀態可以在同一堂課重新提升。

學生應理解為：

> 行為改變，狀態就可以改變。

而不是：

> 犯錯後便永久失去星星。

## 2.3 求助不是錯誤

以下情況原則上不應直接造成狀態下降：

- 已經嘗試後仍需要協助。
- 需要教師再次拆解步驟。
- 需要確認理解是否正確。
- 因個別學習需求而需要較多提示。
- 問題超出學生目前可自行處理的範圍。

評量單位以小組整體運作模式為主，不因單一學生的一次行為直接改變整組狀態，避免學生互相責怪。

## 2.4 系統鼓勵高品質求助

低品質、立即依賴型提問應逐漸下降；經過觀察、嘗試與推理後形成的高品質求助及深度提問應逐漸增加。

---

# 三、研究依據與設計方向

本產品方向與 2026 年系統性回顧〈Help! I Need Somebody: A Systematic Review of Academic Help-Seeking Among Pre-Service Teachers〉的主要發現一致：求助較適合被理解為受情境、動機與人際關係影響的調節歷程，而非固定的人格特質；正式支持是否容易接近、能否即時回應，也會影響實際求助行為。

參考：<https://doi.org/10.1007/s10648-026-10208-6>

因此，本系統優先營造支持性的求助環境，而不是計算或壓低學生詢問教師的次數。

---

# 四、使用者與權限

## 4.1 教師

教師是系統唯一具有管理與寫入權限的角色，可以：

- 設定班級與小組。
- 開始及結束課堂。
- 操作班級燈號。
- 調整各組自主狀態。
- 增加或取消深度提問紀錄。
- 開啟學生前台。
- 查看歷史課堂與學期資料。

## 4.2 學生前台觀眾

學生不需要帳號，也不直接操作系統。學生前台主要顯示於教室投影機或大型螢幕，或者若學生想讀取也可以透過網頁頁面瀏覽。

學生前台只能讀取該堂課允許公開的資訊，不具備任何修改權限，也不顯示教師分析資料、跨班排名或系統設定。

## 4.3 MVP 不支援的角色

- 學生個人帳號。
- 家長帳號。
- 多位教師的分級管理權限。
- 學校行政管理員後台。

---

# 五、預期使用情境

系統是同一個跨裝置 Web 平台，包含兩種介面：

1. **教師後台**：可使用桌面電腦、筆記型電腦、平板或手機操作。
2. **學生前台**：在教室電腦或投影設備上以全螢幕唯讀顯示。

### 5.1 MVP 確認採用情境：單一鏡射螢幕

本教室是教師筆電直接鏡射投影，全班看到的就是教師螢幕本身，沒有第二台獨立的投影裝置。因此 MVP 第一版採用以下方式：

1. 教師與學生共用同一個頁面，預設顯示學生前台內容（班級秩序、小組狀態、求助提示）。
2. 教師點選畫面角落的登入入口，通過權限確認後，同一頁面切換成教師控制台。
3. 教師操作完成後，手動切回學生前台繼續投影。
4. 結束課堂後，頁面自動切回學生前台，顯示「本節課已結束」。

這個情境下不需要「開啟學生畫面」「另一台裝置開啟」這類跨裝置流程，因為從頭到尾只有一個畫面。

### 5.2 可選架構：教師裝置＋獨立投影裝置

若日後改用教師裝置與教室投影機分開顯示（例如教師用自己的手機或筆電操作，投影機接另一台裝置），標準情境如下，此架構暫不是 MVP 第一版的實作重點，但底層資料流程（課堂識別碼、顯示代碼、同步機制）與 5.1 相容，未來要切換不需要重新設計後端：

1. 教師登入平台。
2. 選擇班級並開始一堂課。
3. 系統建立唯一課堂識別碼及短期顯示代碼。
4. 教師按下「開啟學生畫面」。
5. 學生前台在新視窗或另一台裝置開啟。
6. 教師在後台操作，學生前台同步更新。
7. 教師結束課堂，系統保存正式紀錄並使顯示代碼失效。

學生以個人裝置直接進入學生前台，兩種情境下都不在第一版範圍內。

---

# 六、系統架構

## 6.1 架構概覽

> 教師後台／學生前台  
> → Google Apps Script 後端  
> → Google Sheet 中央資料庫

瀏覽器本機另保留暫存與待同步操作，用於改善即時回應及網路短暫中斷時的可靠性。

## 6.2 前端

同一套響應式 Web App 提供兩種顯示模式：

- 教師模式：具備管理與課堂操作功能。
- 學生顯示模式：只讀、大字體、低資訊密度。

MVP 依 5.1 單一鏡射螢幕情境，兩種模式在**同一個頁面、同一個網址**內切換：頁面預設是學生顯示模式，教師通過權限確認後，頁面內容原地切換成教師模式，不開新分頁、不換網址。

若採 5.2 教師裝置＋獨立投影裝置的可選架構，才會需要用網址區分兩種模式，邏輯網址可表示為：

- 教師後台：`?view=teacher`
- 學生前台：`?view=display&token=顯示代碼`

實際網址格式可依 Apps Script 部署方式調整。

## 6.3 後端

Google Apps Script 負責：

- 驗證教師身分與操作權限。
- 以最小必要的唯讀權限讀取教師指定的正式名單 Google Sheet。
- 預覽、驗證並匯入每學期班級與分組設定。
- 驗證學生前台顯示代碼。
- 讀寫 Google Sheet。
- 防止同時寫入造成衝突。
- 驗證資料格式與允許的狀態值。
- 建立課堂、結束課堂及失效顯示代碼。
- 回傳學生前台所需的最小公開資料。

## 6.4 資料庫

Google Sheet 是 MVP 的中央正式資料來源。教師與學生前台都不得直接存取或修改試算表，所有操作必須經過 Apps Script。

## 6.5 本機暫存

瀏覽器本機暫存用於：

- 讓教師點擊後立即看到結果。
- 保存尚未成功送出的操作。
- 網路恢復後重試同步。
- 瀏覽器意外重新整理後，提示恢復尚未完成的課堂。

本機暫存不是跨裝置的正式資料來源；雲端資料仍為最終依據。

---

# 七、資訊架構與主要頁面

## 7.1 教師登入／存取確認

- 確認目前 Google 帳號具有教師權限。
- 無權限時不得載入班級、課堂或歷史資料。
- 顯示清楚的帳號與重新登入指引。

## 7.2 教師首頁

顯示：

- 本學期七個班級。
- 各班是否有進行中的課堂。
- 「開始課堂」入口。
- 歷史紀錄與學期摘要入口。
- 班級、小組及系統設定入口。

## 7.3 班級與小組設定

教師可以：

- 新增或停用班級。
- 設定班級顯示名稱及排序。
- 設定小組數量、名稱及排序。
- 從每學期正式 Google Sheet 匯入班級與分組名單。
- 在匯入前預覽班級、小組及可能的重複項目。
- 選擇「新增」「更新既有設定」或「取消匯入」，不得未經確認直接覆寫。
- 修改後只影響新建立的課堂，不覆寫已結束課堂的歷史名稱快照。

### 7.3.1 兩份 Google Sheet 的用途必須分開

系統使用兩類不同的 Google Sheet：

1. **正式學生名單／分組表**：每學期由教師準備，是該學期班級與小組設定的權威來源，平台只讀取。
2. **平台資料庫**：由系統維護，用來保存班級設定、進行中狀態、課堂紀錄與學期摘要，不供日常人工編輯。

不得直接把正式學生名單表當成進行中課堂的即時資料庫，避免來源格式調整、人工排序或名單修改破壞課堂紀錄。

### 7.3.2 正式名單 Sheet 匯入流程

1. 教師在後台貼上或選擇正式名單 Google Sheet。
2. 系統以唯讀方式取得可用工作表分頁。
3. 教師選擇要匯入的學期與分頁。
4. 系統讀取資料並顯示預覽。
5. 系統檢查必要欄位、空值、重複班級、重複小組與排序問題。
6. 若名單包含學生欄位，系統可檢查同一學生是否重複出現在不同小組。
7. 教師確認新增、更新、停用及警告項目。
8. 後端建立一筆匯入批次，將班級與小組設定寫入平台資料庫。
9. 學生姓名、電子郵件、座號等個人欄位只供匯入預覽與核對，MVP 匯入完成後不保存。

### 7.3.3 建議的正式名單格式

建議採「一位學生一列」格式，以利檢查分組完整性：

| 必要性 | 欄位 | 說明 |
|---|---|---|
| 必要 | `semester_id` | 學期代碼，例如 `115-1` |
| 必要 | `class_name` | 班級名稱，例如「六年三班」 |
| 必要 | `group_name` | 小組名稱，例如「第一組」 |
| 建議 | `student_key` | 校內穩定識別或座號，只供比對，不保存 |
| 建議 | `student_name` | 只供匯入預覽與核對，不保存 |
| 選填 | `student_email` | 只供匯入預覽與核對，不保存 |
| 選填 | `group_sort_order` | 小組顯示順序 |

若不希望名單表包含個人資料，也可以採「一組一列」格式，只提供 `semester_id`、`class_name`、`group_name` 與 `group_sort_order`；此格式可以建立平台設定，但無法檢查學生是否漏分組或重複分組。

欄位名稱可在匯入畫面進行一次對應，不要求來源 Sheet 必須完全使用上述英文名稱。

### 7.3.4 匯入驗證規則

匯入前至少檢查：

- 學期、班級及小組欄位不得為空。
- 同一班級不得有名稱相同但排序不同的重複小組。
- 同一學生在同一班級原則上只能屬於一個課堂經營小組；如正式名單允許多重分組，需由教師確認要採用哪一組。
- 前後空白、全形半形及大小寫差異應先正規化再比對。
- 新增、變更、停用與未變更項目需分開顯示。
- 有警告時可以返回修正來源 Sheet，不得強迫匯入。

### 7.3.5 學期中更新

- 正式名單或分組調整後，由教師手動按下「重新匯入／比對」。
- 系統不得自動在背景覆寫班級設定。
- 系統先顯示與上次匯入的差異，再由教師確認套用。
- 被移出正式名單的小組預設標記為停用，不直接刪除。
- 更新只影響新建立的課堂；已結束課堂保留當時的班級與小組名稱快照。
- 進行中的課堂不接受名單結構更新，需等課堂結束後處理。

### 7.3.6 Google Classroom 的定位

MVP 使用 Google 帳號進行教師登入，但不依賴 Google Classroom 提供正式名單或分組資料。

原因：Classroom 的一般課程與學生名單 API 已可讀取，但 Student Groups API 截至 v2.0 文件日期仍屬 Google Workspace Developer Preview。為降低授權、網域政策與預覽 API 變動造成的開發風險，Classroom 名單整合列為後續可選功能。

未來若加入 Classroom 整合，其定位是協助比對課程與名單差異，不取代每學期正式 Google Sheet 的權威來源。

官方參考：

- Classroom 學生名單：<https://developers.google.com/workspace/classroom/reference/rest/v1/courses.students/list>
- Classroom 學生群組：<https://developers.google.com/workspace/classroom/guides/manage-student-groups>

## 7.4 進行中課堂／教師控制台

依 5.1 單一鏡射螢幕情境，教師從學生前台畫面角落的登入入口進入教師控制台，權限確認方式見 19.1；前端原型階段暫以示範用密碼替代，正式版須改回 Google 帳號授權。

教師在單一畫面完成大部分操作：

- 顯示班級與課堂開始時間。
- 紅、黃、綠燈快捷按鈕：點擊一律新增一筆帶時間戳記的紀錄，不限筆數。
- 本節課燈號歷程：可點選任一筆進入更正模式，修正該筆顏色（詳見九）。
- 每組三種星級快捷按鈕。
- 每組 `💡` 操作角標：點擊一次即新增一筆，誤按透過「最近一次操作復原」修正（不再另外提供「－💡」按鈕，詳見十一）。
- 特殊表現球 🎱：輸入座號，點選「好問題」或「幫助別人」新增一顆球；點擊已加入的球可移除（詳見廿九）。
- 最近一次操作復原。
- 返回投影畫面（切回學生前台）。若採 5.2 獨立投影裝置架構，才需要「開啟或複製學生前台連結」。
- 同步狀態。
- 結束課堂：結束後自動切回學生前台。

## 7.5 學生前台

依 5.1 單一鏡射螢幕情境，學生前台是頁面的**預設畫面**，教師尚未登入前、或登入後按下「返回投影畫面」時都會顯示此畫面，畫面角落保留一個不搶視覺焦點的教師登入入口。

顯示：

- 班級名稱。
- 當前班級燈號及文字標籤。
- 本節課燈號歷程（不限筆數，依時間排列）。
- 所有小組目前自主狀態。
- 各組深度提問數量。
- 特殊表現球 🎱（唯讀，不提供點擊操作，詳見廿九）。
- 求助流程提示。
- 必要時顯示「等待課堂開始」或「本節課已結束」。

## 7.6 歷史紀錄

教師可以：

- 依班級查看過去課堂。
- 查看日期、燈號歷程、各組最終星級及 💡 數量。
- 查看未完成或待同步的異常紀錄。

第一版不要求逐次操作的詳細分析介面。

## 7.7 學期摘要

顯示簡單、可解釋的資料：

- 各班已完成課堂數。
- 班級平均自主狀態。
- 各星級所占比例。
- 深度提問總數與每堂平均。
- 紅、黃、綠燈出現次數與比例。

### 7.7.1 燈號統計計算方式

燈號在一堂課中可能變動、不限筆數（見九、17.3），這個依序記錄的清單本身已經足夠支撐統計分析，不需要另外設計「切換記錄」機制或新增資料表。計算基礎直接使用 `Sessions` 表的 `lights_json` 欄位（17.3 節），逐筆攤開來看。

MVP 採兩種統計角度：

1. **出現比例**：把範圍內所有已完成課堂的 `lights_json` 攤平計算每一筆，依 green／yellow／red 分類計數與百分比。
2. **每堂課結束狀態分布**：每堂課取 `lights_json` 中時間最晚的一筆（即該堂課最終燈號），統計範圍內各終點顏色的堂數與百分比，用來反映「這些課大多是怎麼收尾的」。

`lights_json` 為空（完全沒記錄過燈號）的課堂不計入以上兩種統計。

「燈號切換方向統計」（例如「綠→紅」「紅→綠」的轉換次數，用來看秩序變好或變糟的動態）在概念上同樣可以從既有的三格序列算出，但第一版不實作，列為未來可選功能（見廿七）。

教師端可以查看七班資料，但平時不得在學生前台公開七班即時排名。

---

# 八、課堂開始流程

1. 教師在首頁選擇班級。
2. 系統檢查該班是否已有進行中的課堂。
3. 若沒有，教師按下「開始本節課」。
4. 系統建立 `session_id`、顯示代碼、開始時間與版本號。
5. 燈號初始化為尚未記錄。
6. 所有有效小組初始化為 ⭐⭐⭐。
7. 所有小組的 💡 初始化為 0。
8. 教師進入課堂控制台。
9. 教師可開啟學生前台。

同一班級同一時間只能有一堂進行中的課。若偵測到未結束課堂，系統應讓教師選擇「繼續原課堂」或「確認結束原課堂後建立新課堂」，不得直接建立重複課堂。

---

# 九、班級秩序燈號

## 9.1 基本規則

教師可以快速點選：

- 🟢 綠燈
- 🟡 黃燈
- 🔴 紅燈

每次點擊都新增一筆帶時間戳記的紀錄，附加在本節課燈號清單最後面，不限筆數（原規格的「固定三格」已取消，理由見 0.2）。

學生前台顯示：

- 目前燈號（清單中最新一筆）及對應的語意文字。
- 本節課完整燈號歷程，依時間先後排列，每筆附時間戳記。

目前狀態取燈號清單中最新一筆。尚無紀錄時顯示「尚未設定」。

為了讓學生更直接理解每個顏色代表的狀態，前台除了燈號名稱，另外顯示語意文字：

| 燈號 | 語意文字 |
|---|---|
| 🟢 綠燈 | 目前很專注音量很適當 |
| 🟡 黃燈 | 注意小組討論不超過第三人聽 |
| 🔴 紅燈 | 目前像進到菜市場 |

## 9.2 誤按與更正處理

- **新增最新一筆**：教師點擊顏色按鈕一律新增一筆新紀錄，透過「最近一次操作復原」可以撤銷剛新增的那一筆。
- **更正既有一筆**：教師端的燈號歷程清單中，每一筆都可以點擊；點選後進入更正模式並提示目前顏色，教師再點選正確顏色即可原地覆寫該筆的顏色，不會新增筆數、也不會更動該筆的時間戳記；再點一次同一筆可取消更正。
- 課堂結束後，MVP 不在一般介面提供直接修改；如需修正，列為管理性資料維護流程。

## 9.3 無障礙顯示

燈號不得只依賴顏色。畫面同時顯示圖形與「綠燈／黃燈／紅燈」文字。

---

# 十、小組自主解題狀態

## 10.1 ⭐⭐⭐ 自主合作

小組能互相協助並共同推進問題解決；大部分一般問題可透過閱讀、嘗試或討論處理，必要時才尋求教師協助。

可能行為：

- 先閱讀畫面、教材或提示。
- 先自己操作或嘗試。
- 卡住時先與組員討論。
- 組員知道答案時會協助說明。
- 詢問教師時能描述嘗試與卡點。

⭐⭐⭐ 不代表完全不能詢問教師。

## 10.2 ⭐⭐ 發展中

小組已能互相協助，但部分一般性問題仍較依賴教師。

可能行為：

- 已出現小組互助。
- 部分學生仍會很快尋求教師。
- 經提醒先看提示或先問組員後，多半可以繼續。
- 自主處理問題的習慣尚未穩定。

## 10.3 ⭐ 需要較多支持

小組目前大部分卡點仍需要教師介入才能繼續，自主嘗試與組員互助尚未形成穩定習慣。

可能行為：

- 一遇到問題就直接尋求教師。
- 很少先閱讀提示或嘗試。
- 很少詢問組員。
- 教師協助一個步驟後，很快再次停止。
- 重複出現已說明過的基礎問題。

此狀態仍可在同一堂課重新提升。

## 10.4 操作規格

- 教師後台不另外放置「一星／二星／三星」文字按鈕；每組卡片中的三顆星本身就是操作控制。
- 點擊第 1 顆星直接設為 ⭐，點擊第 2 顆直接設為 ⭐⭐，點擊第 3 顆直接設為 ⭐⭐⭐。
- 不使用點擊整排後依序循環的操作，避免多按一次或從 ⭐ 誤跳回 ⭐⭐⭐。
- 星星圖形統一使用 ⭐，已選範圍完整顯示、未選範圍淡化處理（不使用空心星型符號，因為顏色對 emoji 圖形沒有作用，只有透明度看得出差異）；目前等級另以文字標籤呈現，不能只靠星星圖形判讀。
- 文字標籤依星級對應：⭐⭐⭐「有效的自主合作中！」、⭐⭐「逐漸進入軌道」、⭐「需要較多支持」。
- 小組達到 ⭐⭐⭐ 時，該小組卡片底色換成淡鵝黃色；⭐ 時卡片底色換成深灰色（卡片內文字同步改為淺色，維持對比）；教師後台與學生前台顯示邏輯一致，一眼可辨識需要留意或表現良好的小組。
- 每顆星在手機上需提供至少約 44 × 44 像素的有效觸控範圍。
- 教師點擊星星後立即更新教師畫面，再同步至雲端與學生前台。
- 學生前台中的星星維持唯讀，不提供點擊操作。
- 系統保存進行中的目前星級及課堂結束時的最終星級。
- 學期統計使用最終星級；操作歷程只供系統復原與偵錯，不作學生評分。

---

# 十一、深度提問 💡

💡 是額外成就，不是第四顆星，也不會抵銷或取代自主狀態。

可能觸發條件：

1. 經過嘗試後發現新的問題。
2. 比較不同方法。
3. 詢問背後原因。
4. 延伸原本任務。
5. 清楚描述已完成的嘗試與目前卡點。

核心精神：

> 不是問題比較難就算，而是學生經過觀察、嘗試或推理後，產生值得深入討論的問題。

## 11.1 MVP 操作規格

- 每組提供單一個 `💡` 操作角標（不再另外提供「－💡」按鈕），教師點擊一次即新增一筆。
- 誤按時透過「最近一次操作復原」修正，不提供獨立的減少按鈕。
- 教師後台的 `💡` 角標本身兼具顯示與操作功能：尚未新增過時呈現淡化（半透明）樣式提示可點擊，新增第 1 筆後轉為正常顯示，達 2 筆以上才顯示 `× N` 數量。
- 學生前台在數量為 1 時顯示 `💡`，大於 1 時顯示 `💡 × N`。
- MVP 只記錄數量，不要求輸入問題文字或學生姓名。

---

# 十二、學生求助提示

學生前台固定顯示簡化流程：

> 👂 先聽 → 👀 先看課本講義 → 🧪 先試 → 👥 互相幫忙 → 🙋 再找老師

此流程是原則，不是硬性限制。不得設計成完成前四步以前禁止詢問教師。

教師保留依學生狀況直接提供支持的彈性。

---

# 十三、教師後台操作需求

## 13.1 課堂操作速度

- 常用操作應在 1 次點擊完成。
- 非常用或可能造成資料遺失的操作最多 2 次確認。
- 不要求教師在授課中輸入文字。
- 手機操作按鈕需足夠大，避免誤觸。
- 星級直接在小組卡片的星星位置設定，不另開選單或對話框。

## 13.2 防止誤操作

- 目前已選狀態需有清楚視覺標示。
- 重要操作後提供短暫復原入口。
- 結束課堂需要確認。
- 不使用只靠滑動手勢才能完成的核心操作。

## 13.3 同步狀態提示

教師後台固定顯示以下其中一種狀態：

- 已同步。
- 同步中。
- 離線，操作等待同步。
- 同步失敗，請重試。

不得在尚未寫入雲端時顯示「已儲存」。

---

# 十四、學生前台顯示需求

## 14.1 版面區塊

### A. 班級秩序區

畫面最大、最醒目，顯示：

- 班級名稱。
- 當前燈號。
- 當前燈號文字。
- 本節課燈號歷程（不限筆數，依時間排列）。

### B. 小組自主狀態區

顯示全部小組：

| 小組 | 自主狀態 | 深度提問 |
|---|---|---|
| 第一組 | ⭐⭐⭐ |  |
| 第二組 | ⭐⭐ | 💡 |
| 第三組 | ⭐⭐⭐ |  |
| 第四組 | ⭐ |  |
| 第五組 | ⭐⭐⭐ | 💡 × 2 |

實際畫面可使用卡片式配置，但資訊語意需維持一致。

### C. 求助提示區

固定顯示：

> 👂 先聽 → 👀 先看課本講義 → 🧪 先試 → 👥 互相幫忙 → 🙋 再找老師

### D. 特殊表現區

顯示本節課所有特殊表現球（🎱，詳見廿九），唯讀、不提供點擊操作。

## 14.2 投影可讀性

- 字體必須能從教室後方閱讀。
- 燈號與星星需具備高對比。
- 避免大量小字、複雜圖表及不必要動畫。
- 依小組數量自動調整排列，但不得小到難以辨識。
- 支援瀏覽器全螢幕顯示。

## 14.3 前台狀態

學生前台至少需處理：

- 等待課堂開始。
- 課堂進行中。
- 暫時無法同步。
- 課堂已結束。
- 顯示代碼無效或過期。

暫時無法同步時保留最後一次成功資料，並顯示低干擾提示，不應清空整個畫面。

---

# 十五、跨裝置同步規格

## 15.1 中央狀態

進行中的正式狀態保存在 Google Sheet，由 Apps Script 統一讀寫。各裝置不得假設自己的本機狀態永遠最新。

## 15.2 同步頻率

- 教師操作完成後立即送出更新。
- 發出操作的教師裝置可先進行樂觀更新，失敗時必須回復或標示待同步。
- 學生前台預設每 3–5 秒取得最新狀態。
- 使用者切回頁面時立即重新同步。
- 課堂結束時立即同步最終狀態。

同步頻率可依實測調整，但不得造成明顯延遲或不必要的 API 流量。

## 15.3 版本與衝突

每堂課保存遞增的 `revision`。每次成功變更後版本加一，回傳完整或必要的最新狀態。

Apps Script 寫入共享資料時必須使用鎖定機制，避免兩個裝置同時修改相同課堂資料造成覆蓋。

若客戶端送出的版本已過期：

1. 後端不得靜默覆蓋較新的資料。
2. 後端回傳目前最新狀態。
3. 可安全重播的動作由系統重新套用。
4. 無法安全合併的操作要求教師重新確認。

## 15.4 重複請求

每次寫入操作應包含唯一 `action_id`。若裝置因網路問題重送相同操作，後端不得重複增加 💡 或重複加入燈號。

## 15.5 多個教師控制裝置

MVP 允許教師在另一台已授權裝置重新開啟同一堂課，但主要使用情境仍是一台教師控制裝置。

如果同一堂課同時有兩個可寫入控制台，兩者都必須看見同步狀態與最新版本；學生前台始終維持唯讀。

---

# 十六、網路中斷與錯誤恢復

## 16.1 教師裝置離線

- 操作立即寫入本機待同步佇列。
- 畫面清楚顯示離線狀態。
- 網路恢復後依 `action_id` 順序重送。
- 無法安全合併時提示教師確認，不得無聲遺失操作。

## 16.2 學生前台離線

- 保留最後一次成功同步畫面。
- 顯示簡短的重新連線提示。
- 網路恢復後自動取得完整最新狀態。

## 16.3 結束課堂時同步失敗

- 本機將課堂標記為「已結束、待同步」。
- 不得顯示為雲端已完成儲存。
- 網路恢復後自動重試。
- 教師首頁需顯示待同步警示，直到伺服器確認完成。

## 16.4 瀏覽器意外關閉

重新開啟後若偵測到本機未完成狀態或伺服器仍有進行中課堂，系統應引導教師繼續，而不是自動建立另一堂課。

---

# 十七、Google Sheet 資料結構

## 17.1 `Classes`

| 欄位 | 說明 |
|---|---|
| `class_id` | 班級唯一識別碼 |
| `semester_id` | 所屬學期 |
| `class_name` | 顯示名稱 |
| `sort_order` | 排序 |
| `source_record_key` | 正式名單來源識別；手動建立時可為空 |
| `import_source` | `official_sheet` 或 `manual` |
| `is_active` | 是否啟用 |
| `created_at` | 建立時間 |
| `updated_at` | 更新時間 |

## 17.2 `Groups`

| 欄位 | 說明 |
|---|---|
| `group_id` | 小組唯一識別碼 |
| `class_id` | 所屬班級 |
| `group_name` | 小組顯示名稱 |
| `sort_order` | 排序 |
| `source_record_key` | 正式名單來源識別；手動建立時可為空 |
| `import_source` | `official_sheet` 或 `manual` |
| `is_active` | 是否啟用 |
| `created_at` | 建立時間 |
| `updated_at` | 更新時間 |

## 17.3 `Sessions`

| 欄位 | 說明 |
|---|---|
| `session_id` | 課堂唯一識別碼 |
| `class_id` | 班級識別碼 |
| `class_name_snapshot` | 當時班級名稱 |
| `lesson_date` | 上課日期 |
| `started_at` | 開始時間 |
| `ended_at` | 結束時間 |
| `status` | `active`、`completed`、`pending_sync`、`cancelled` |
| `lights_json` | 本節課燈號歷程，JSON 陣列，每筆 `{color, at}`（顏色、時間戳記），不限筆數 |
| `achievements_json` | 本節課特殊表現球紀錄，JSON 陣列，每筆 `{seat, type, at}`（座號、`question` 或 `help`、時間戳記），不限筆數；隨堂課結束歸零，不做跨堂累積（見廿九） |
| `revision` | 課堂狀態版本 |
| `display_token_hash` | 顯示代碼雜湊或安全識別 |
| `display_expires_at` | 顯示代碼到期時間 |
| `created_by` | 建立者識別 |
| `updated_at` | 最後更新時間 |

## 17.4 `SessionGroups`

| 欄位 | 說明 |
|---|---|
| `session_group_id` | 唯一識別碼 |
| `session_id` | 課堂識別碼 |
| `group_id` | 小組識別碼 |
| `group_name_snapshot` | 當時小組名稱 |
| `initial_stars` | 初始星級，固定為 3 |
| `current_stars` | 進行中目前星級 |
| `final_stars` | 結束時最終星級 |
| `bulb_count` | 本堂深度提問數量 |
| `updated_at` | 最後更新時間 |

## 17.5 `SessionEvents`

此分頁只記錄系統操作，用於同步、防重複、復原及偵錯；不是學生個人提問紀錄。

| 欄位 | 說明 |
|---|---|
| `action_id` | 操作唯一識別碼 |
| `session_id` | 課堂識別碼 |
| `event_type` | 燈號、星級、💡、開始、結束等 |
| `target_id` | 目標小組或課堂 |
| `previous_value` | 操作前值 |
| `new_value` | 操作後值 |
| `device_id` | 發出操作的裝置識別 |
| `created_at` | 操作時間 |
| `applied_revision` | 套用後版本 |

## 17.6 `Settings`

| 欄位 | 說明 |
|---|---|
| `key` | 設定鍵 |
| `value` | 設定值 |
| `updated_at` | 最後更新時間 |

## 17.7 `ImportBatches`

保存每次正式名單匯入結果，不保存學生個人列資料。

| 欄位 | 說明 |
|---|---|
| `import_id` | 匯入批次唯一識別碼 |
| `semester_id` | 匯入學期 |
| `source_spreadsheet_id` | 來源 Google Sheet 識別碼 |
| `source_sheet_name` | 來源分頁名稱 |
| `source_schema_version` | 匯入格式版本 |
| `source_data_hash` | 正規化後來源資料雜湊，用於差異偵測 |
| `imported_at` | 匯入時間 |
| `imported_by` | 操作者識別 |
| `status` | `previewed`、`completed` 或 `failed` |
| `classes_added` | 新增班級數 |
| `groups_added` | 新增小組數 |
| `records_updated` | 更新數量 |
| `warnings_count` | 警告數量 |

## 17.8 資料規則

- 系統不得依賴工作表列號作為永久識別碼。
- 所有 ID 由後端產生且不得重複。
- 日期時間以一致格式保存，介面再轉換為臺北時區顯示。
- 已結束課堂保存班級與小組名稱快照，避免後續改名影響歷史。
- 不保存學生姓名、特殊學習需求或個人行為紀錄。
- 正式名單 Google Sheet 與平台資料庫使用不同檔案，並採唯讀來源、受控寫入資料庫的方式處理。
- 來源 Sheet ID 與匯入紀錄只顯示於教師管理介面，不顯示於學生前台。

---

# 十八、後端功能介面

MVP 至少需要下列邏輯操作：

- 取得教師可用的班級與設定。
- 驗證教師是否具有正式名單 Google Sheet 的讀取權限。
- 列出來源 Sheet 的工作表分頁。
- 讀取並正規化正式名單資料。
- 預覽、驗證及比對匯入差異。
- 確認套用匯入結果並建立匯入批次紀錄。
- 建立、修改與停用班級及小組。
- 開始課堂。
- 取得進行中課堂狀態。
- 新增、復原或更正燈號。
- 設定小組星級。
- 增減小組 💡。
- 取得學生前台公開狀態。
- 結束課堂。
- 列出班級歷史課堂。
- 取得學期摘要。
- 重送或查詢待同步操作。

後端必須驗證：

- 教師權限。
- 顯示代碼有效性。
- 課堂是否仍在進行。
- 星級是否只能為 1、2 或 3。
- 燈號是否只能為 green、yellow 或 red。
- 燈號紀錄不得超過三筆。
- 💡 不得小於 0。
- `action_id` 是否已處理。
- `revision` 是否仍為可接受版本。

---

# 十九、權限、安全與隱私

## 19.1 教師後台

- 教師後台必須登入授權的 Google 帳號。
- 可採指定帳號白名單或學校 Google Workspace 網域限制。
- 所有寫入權限由後端判斷，不得只依賴前端隱藏按鈕。

## 19.2 學生前台

- 學生前台使用高強度、不可猜測的短期顯示代碼。
- 顯示代碼只允許讀取單一進行中課堂的公開狀態。
- 課堂結束後代碼失效。
- 前台不得回傳教師帳號、歷史資料或其他班級資料。

## 19.3 Google Sheet

- 試算表維持私人，不公開分享。
- 瀏覽器端不得包含服務帳號金鑰或可直接修改 Sheet 的長期憑證。
- 所有輸入由 Apps Script 驗證與清理。
- 定期建立備份或匯出副本。

## 19.4 資料最小化

MVP 只記錄班級、小組與課堂層級資料，不記錄學生姓名、個別提問次數、特殊需求或個人評分。

---

# 二十、學期累積與期末評比

## 20.1 平時資料

系統保存每班每堂課資料，用於觀察：

- 小組自主狀態變化。
- 班級整體自主程度。
- 深度提問出現情形。
- 班級秩序長期狀況。

## 20.2 自主狀態基礎換算

暫定：

- ⭐⭐⭐ = 3
- ⭐⭐ = 2
- ⭐ = 1

可計算該班所有小組、所有已完成課堂的平均自主狀態。

## 20.3 深度提問

💡 保存數量，但具體加分權重在實際使用 2–4 週後再決定。MVP 不以尚未驗證的權重自動產生正式期末成績。

## 20.4 燈號與星級

兩者分開記錄：

- 燈號衡量班級秩序。
- 星級衡量學習與求助方式。

是否合併為期末總評，待實際資料驗證後決定。

## 20.5 七班期末分級

平時不公開七班即時排名。若期末確認採用整體評比，暫定：

- Level 3：第 1–2 名。
- Level 2：第 3–5 名。
- Level 1：第 6–7 名。

三個 Level 都應有獎勵，差別僅在獎勵程度。正式計分公式不屬於本次 MVP 驗收條件。

---

# 二十一、MVP 必做範圍

1. 七個班級設定。
2. 從每學期正式 Google Sheet 匯入班級、小組名稱及排序。
3. 匯入欄位對應、資料預覽、錯誤檢查及差異確認。
4. 學期中可重新匯入並比對新增、更新與停用項目。
5. 各班小組數量、名稱及排序設定。
6. 教師 Google 帳號存取控制與正式名單 Sheet 唯讀權限。
7. 開始一堂課。
8. 同一班只能有一堂進行中課堂。
9. 紅、黃、綠燈快速操作。
10. 每堂課燈號可不限次數記錄，並可點選既有紀錄更正顏色。
11. 所有小組初始化為 ⭐⭐⭐。
12. 教師可直接設定 ⭐⭐⭐、⭐⭐ 或 ⭐。
13. 每組可增加、減少及復原 💡。
14. 教師後台響應式介面。
15. 學生唯讀投影前台。
16. 教師後台與學生前台跨裝置同步。
17. 同步中、離線與失敗狀態提示。
18. 網路恢復後重送待同步操作。
19. 結束課堂並保存正式紀錄。
20. 查看單一班級過去課堂紀錄。
21. 顯示簡單學期累積資料。
22. Google Sheet 中央資料儲存。
23. Apps Script 權限驗證、資料驗證、防重複及鎖定寫入。
24. 個別學生特殊表現球 🎱：輸入座號記錄「好問題」或「幫助別人」，教師端可移除誤植紀錄並支援復原（詳見廿九）。

---

# 二十二、第一版暫不製作

- 學生個人帳號。
- 個人扣分或個人排行榜（特殊表現球 🎱 是單堂課即時展示，不是排行榜或計分系統，見廿九）。
- 特殊表現球的學生名冊／姓名對照：座號只是輸入用的識別方式，不對應真實姓名。
- 特殊表現球的跨堂課、學期累積排行榜（是否新增待廿七驗證）。
- 學生以個人裝置加入課堂。
- 七班即時公開排行榜。
- 金幣、商店、虛擬道具或徽章。
- 家長登入。
- AI 自動評分或判斷問題品質。
- 複雜統計圖表。
- 即時記錄每一次學生提問內容。
- 學生姓名與特殊學習需求資料。
- 多校、多租戶或付費方案。
- 原生 iOS、Android 或 Windows 應用程式。
- 已結束課堂的完整後台編輯工具。

---

# 二十三、使用體驗與視覺原則

## 23.1 狀態優先

學生前台優先回答：

> 現在發生什麼？

學期分析只出現在教師後台。

## 23.2 手機優先的教師操作

- 常用按鈕具有足夠觸控面積。
- 不需要水平捲動才能完成核心操作。
- 班級燈號在畫面上方保持容易觸及。
- 小組操作可快速掃視並直接點選。

## 23.3 投影優先的學生畫面

- 大字體、高對比、低資訊密度。
- 不使用只靠顏色傳達的訊息。
- 避免閃爍、過度動畫與造成羞辱感的效果。
- 狀態下降不播放懲罰性音效。

## 23.4 語言

- 採用支持性、描述目前狀態的文字。
- 避免「失敗」「扣分」「拖累全組」等標籤。
- ⭐ 顯示為「需要較多支持」，不顯示為「最差」。

---

# 二十四、MVP 驗收條件

## 24.1 功能驗收

1. 教師能使用授權帳號進入後台，未授權使用者不能修改資料。
2. 教師能選擇每學期正式 Google Sheet 及其中的資料分頁。
3. 教師能對應來源欄位並匯入班級、小組名稱與排序。
4. 匯入前能看到錯誤、警告與差異，且不會未經確認覆寫既有班級或小組。
5. 名單中的學生個人欄位不會寫入平台正式資料表。
6. 教師能建立七班及其小組設定。
7. 教師能在手機或桌面瀏覽器開始課堂。
8. 新課堂所有小組自動為 ⭐⭐⭐，💡 為 0，燈號為空。
9. 教師能以一次點擊新增燈號，第四筆操作被阻止並清楚提示。
10. 教師能調整任一小組星級，包含向上恢復。
11. 教師能增加或取消 💡，數量不得小於 0。
12. 學生前台只能查看，不能修改。
13. 教師操作後，學生前台在正常網路下於合理時間內更新。
14. 兩個裝置接近同時操作時，不會產生重複 💡、第四筆燈號或損壞資料。
15. 相同 `action_id` 重送時只套用一次。ㄨㄟ
16. 網路中斷時教師能看見離線狀態，操作不會無聲遺失。
17. 網路恢復後待同步操作可完成，或清楚要求教師處理衝突。
18. 結束課堂後保存燈號、各組最終星級及 💡 數量。
19. 結束課堂後原學生顯示代碼失效或只顯示課堂已結束。
20. 歷史頁能正確查看指定班級的已完成課堂。
21. 學期摘要計算結果可由 Sheet 原始資料人工核對。

## 24.2 裝置與瀏覽器驗收

至少測試：

- Windows 桌面版 Chrome 或 Edge。
- Android Chrome 或 iPhone Safari，依教師實際手機決定優先順序。
- 1920×1080 教室投影畫面。
- 手機直向教師控制畫面。
- 教師後台與學生前台同時開啟。

## 24.3 課堂可用性驗收

- 教師不需直接開啟 Google Sheet。
- 課堂中的常用操作不要求文字輸入。
- 教師能在不中斷授課的情況下完成燈號、星級與 💡 操作。
- 學生可從教室後方辨識目前燈號與小組狀態。

---

# 二十五、開發流程

## 階段 1：規格與原型

- 確認 v2.0 需求。
- 製作教師後台手機版與桌面版線框圖。
- 製作學生投影前台線框圖。
- 以模擬課堂流程驗證按鈕位置和資訊量。

## 階段 2：雲端基礎

- 建立開發用 Google Sheet。
- 建立 Apps Script 專案。
- 實作教師權限與資料驗證。
- 定義正式名單 Google Sheet 範本與欄位對應規則。
- 實作正式名單 Sheet 的唯讀存取、預覽、驗證與差異匯入。
- 實作 `ImportBatches` 紀錄及匯入失敗復原。
- 實作資料表初始化、課堂建立及讀取。
- 建立測試部署，不直接使用正式資料。

## 階段 3：即時課堂 MVP

- 實作教師控制台。
- 實作學生前台。
- 實作燈號、星級與 💡。
- 實作版本、鎖定、防重複與同步狀態。
- 實作結束課堂。

## 階段 4：歷史與摘要

- 班級歷史紀錄。
- 學期摘要。
- 基本資料核對與匯出。

## 階段 5：可靠性與課堂測試

- 手機與投影同步測試。
- 網路中斷及恢復測試。
- 重複點擊與同時寫入測試。
- 實際教室投影可讀性測試。
- 建立正式版本化部署與備份流程。

## 階段 6：2–4 週試用

- 收集教師操作負擔與遺忘調整狀態的情形。
- 觀察學生是否互相責怪。
- 觀察 💡 是否改善提問品質。
- 驗證星級判斷的一致性。
- 再決定期末評比公式與後續功能。

---

# 二十六、開發環境與資料分離

- 開發與正式環境使用不同 Google Sheet。
- 測試資料不得混入正式課堂紀錄。
- 正式 Web App 使用版本化部署。
- 每次資料結構變更需記錄版本及遷移方式。
- 正式表格至少提供定期副本或匯出備份。
- 前端錯誤不得顯示 Sheet ID、內部欄位或敏感技術資訊給學生前台。

---

# 二十七、實際使用後仍需驗證

1. ⭐⭐⭐／⭐⭐／⭐ 是否容易且一致地判斷。
2. 教師是否因忙碌而忘記調整狀態。
3. 學生是否會責怪造成狀態下降的同學。
4. 💡 是否真的提高問題品質。
5. 💡 應持續計數，還是只顯示本堂有／無。
6. 自主狀態與燈號是否應納入同一個期末獎勵。
7. 小組能力差異是否造成長期固定結果。
8. 學生前台 3–5 秒更新是否足夠。
9. 教師是否需要同時使用兩個可寫入控制裝置。
10. 學生前台是否需要無 Google 登入即可開啟。
11. 教室網路品質是否需要更完整的離線模式。
12. Google Sheet 與 Apps Script 是否持續足以支援實際使用量。
13. 燈號切換方向統計（例如班級從紅燈回穩到綠燈的次數）是否值得新增，待學期資料累積後再評估。
14. 特殊表現球 🎱 的跨堂課、學期累積排行榜是否值得新增，待學期資料累積後再評估。

這些問題應以實際課堂資料與使用經驗調整，不在 MVP 開發前一次決定完畢。

---

# 二十八、MVP 成功條件

如果系統運作良好，應觀察到：

- 教師不必反覆口頭說明目前班級狀態。
- 教師能直接在平台完成操作，不需要操作 Google Sheet。
- 教師後台與學生前台能在不同裝置穩定配合。
- 「老師！接下來要幹嘛？」等立即依賴型問題逐漸下降。
- 學生開始主動查看提示及詢問組員。
- 教師收到的問題逐漸更具體。
- 學生不會因需要協助而害怕提問。
- 教師每堂課增加的管理負擔很低。
- 資料可以可靠保存並供學期回顧。

最重要的成功指標不是：

> 問題數量最低。

而是：

> **低品質、立即依賴型提問下降，同時高品質求助與深度提問增加。**

---

# 二十九、特殊表現球（個別學生表現）🎱

## 29.1 定位

在小組層級的星級（十）與 💡 深度提問（十一）之外，另外提供**個別學生**層級的表揚機制，用來標記「問出好問題」或「積極幫助別人」這兩種值得公開表揚的行為，跟哪一組無關，是完全獨立的一份紀錄。

這不是排行榜或計分系統，屬於 22.「第一版暫不製作」中「個人扣分或個人排行榜」的例外個案，差別在於：

- 只做單堂課即時展示，不做跨堂課累積或排名（是否要做跨堂課累積列入廿七待驗證項目）。
- 不記錄學生姓名，只用座號標示，不建立學生名冊。

## 29.2 兩種類型

| 類型 | 對應行為 | 視覺樣式 |
|---|---|---|
| 好問題（`question`） | 問出跟才智、深度思考相關的好問題 | 花色球：球體上下留白、中間一條顏色帶 |
| 幫助別人（`help`） | 積極協助同學 | 實心球：整顆填滿顏色 |

球上顯示座號數字。球的顏色只用來區分不同座號、方便肉眼辨識同一位學生的多顆球，跟顏色本身沒有語意（不代表好壞）。色票採固定 8 色循環（依 `(座號-1) % 8` 決定），一般班級座號不超過 26 號，大約循環 3 輪。

## 29.3 操作規格

- 教師後台提供一個座號輸入框＋兩顆按鈕（🎱 好問題／⚫ 幫助別人）；輸入座號後點選類型即可新增一顆球，附加在本節課球清單最後面，不限顆數。
- 座號留空或非正整數時，點擊按鈕不動作。
- 已加入的球可以直接點擊移除（例如座號打錯了），移除後可透過「最近一次操作復原」救回原位置。
- 學生前台同步顯示同一份球清單，唯讀、不提供點擊操作。
- 這批資料跟燈號、星級一樣，隨堂課結束清空，不做跨堂課累積。

## 29.4 MVP 範圍外

- 不做學生名冊／姓名對照，座號只是輸入用的識別方式。
- 不做跨堂課、學期累積排行榜。
- 不提供刪除以外的編輯功能（例如改座號、改類型），如果打錯，直接移除再重新輸入。
